<%= form_with(model: player, local: true) do |form| %>
  <% if player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
      <% player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :player_name %>
    <%= form.text_field :player_name, id: :player_player_name %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.number_field :height, id: :player_height %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight, id: :player_weight %>
  </div>

  <div class="field">
    <%= form.label :born %>
    <%= form.number_field :born, id: :player_born %>
  </div>

  <div class="field">
    <%= form.label :college %>
    <%= form.text_field :college, id: :player_college %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
